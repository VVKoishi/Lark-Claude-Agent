[project]
name = "lark-claude-agent"
version = "0.1.0"
description = "A Claude agent that integrates with Lark (Feishu)."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "claude-agent-sdk>=0.1.20",
    "lark-oapi>=1.5.2",
    "python-dotenv>=1.0.0",
]
